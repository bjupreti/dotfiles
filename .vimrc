syntax on
set number
set tabstop=2 " The tabstop setting controls the width of the tab character (\t) when expandtab is disabled and the number of expand tab to be inserted when expandtab is enabled.
set expandtab " Uses the appropriate number of spaces to insert instead of the tab character (\t) when the tab key is hit.
